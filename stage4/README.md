# Stage-4

Stage-4 是**全局（repo 级）**的汇总/作图：扫描所有 `*_newlayout/*/summaries/sentiment_significance.csv`，生成 `global_summaries/` 下的汇总表与可选图表。

- CLI 入口（根目录）：`stage4_global_summaries.py`
- 实现：`stage4/global_summaries.py`
