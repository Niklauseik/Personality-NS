# McNemar 与 Stuart–Maxwell 显著性检验（配对预测）：完整计算案例 + 逐步解释

> 本文只讨论：**给定配对列联表 counts 后**，如何从头算出 p-value，并解释每一步的统计含义。  
> 适用于：对同一批样本，比较两套预测（模型A vs 模型B，或同一模型不同 run）。

---

## 0) counts 是什么？

`counts` 是一个**配对列联表（paired contingency table）**：

- 行：模型 A 的预测类别
- 列：模型 B 的预测类别
- counts[i,j]：A 预测为 i 且 B 预测为 j 的样本数

它长得像“混淆矩阵”，但**不是**“真值 vs 预测”的混淆矩阵；它是“模型A预测 vs 模型B预测”的配对统计。

---

## 1) McNemar（2×2，二分类）——从 counts 到 p-value（Exact）

### 1.1 输入：2×2 配对列联表

二分类时：

| A\B | 0 | 1 |
|---|---:|---:|
| **0** | a | b |
| **1** | c | d |

含义：

- a：A=0 且 B=0（一致）
- d：A=1 且 B=1（一致）
- b：A=0 但 B=1（不一致，方向1）
- c：A=1 但 B=0（不一致，方向2）

**McNemar 只看 b 与 c**（两种不一致方向），因为它要检验“不一致方向是否对称”。

---

### 1.2 原假设 H0（检验目标）

**H0：在“发生不一致”的样本里，两种不一致方向是等概率的（50% vs 50%）。**

定义：

- 不一致总数：$n = b + c$

在 H0 下，可以把“落在 b 方向”看作抛硬币的“正面”：

- $X \sim \text{Binomial}(n, 0.5)$
- 其中 $X$ 表示在 $n$ 个不一致样本里，有多少个落在 b 方向

---

### 1.3 p-value（双侧 exact）的计算公式

令 $m = \min(b, c)$。

双侧 p-value 常用写法：

- 先算单侧尾部：$P(X \le m)$
- 再乘 2（因为 p=0.5 的二项分布对称，另一侧 $P(X \ge n-m)$ 同样大）

所以：

- $p = 2 \cdot P(X \le m)$

展开为组合数求和（可手算）：

- $P(X \le m) = \sum_{k=0}^{m} \binom{n}{k} / 2^n$

---

### 1.4 完整手算案例（给出具体数字）

#### 案例：counts = [[70, 9], [1, 20]]

把表写出来：

| A\B | 0 | 1 |
|---|---:|---:|
| **0** | 70 | 9 |
| **1** | 1 | 20 |

抽取关键值：

- $b = 9$
- $c = 1$
- $n = b + c = 10$
- $m = \min(9, 1) = 1$

解释：

- 一共有 10 个样本 A 与 B 不一致  
- 其中 9 个“不一致”都偏向同一个方向（b 方向），只有 1 个在另一方向（c 方向）  
- 若 H0（随机导致、方向50/50）成立，这种偏差应当很罕见

---

#### Step 1：计算 $P(X \le 1)$

在 $X \sim \text{Binomial}(10, 0.5)$ 下：

$$
P(X \le 1) = [\binom{10}{0} + \binom{10}{1}] / 2^{10}
$$

组合数：

- $\binom{10}{0} = 1$
- $\binom{10}{1} = 10$

所以：

$$
P(X \le 1) = (1 + 10)/1024 = 11/1024 = 0.0107421875
$$

---

#### Step 2：双侧 p-value

$$
p = 2 \cdot 0.0107421875 = 0.021484375
$$

---

#### 结论（如何解释“显著”）

若设显著性水平 $\alpha = 0.05$：

- 因为 $p \approx 0.0215 < 0.05$，拒绝 H0

含义：

- 在“两个方向本应 50/50 随机波动”的前提下，出现“9 vs 1”这种不对称的概率只有约 2.15%  
- 因而我们有统计证据认为：A 与 B 的预测差异不是纯随机噪声导致，而是存在系统性差异（至少体现在“不一致方向”上）

---

## 2) Stuart–Maxwell（K×K，K>2 多分类）——从 counts 到 p-value

> Stuart–Maxwell 检验的是：两套预测的**边际分布是否一致（marginal homogeneity）**。  
> 即：A 对每个类别的“预测总次数”与 B 对每个类别的“预测总次数”是否一致。

---

### 2.1 原假设 H0（检验目标）

设共有 K 类。

定义：

- 行和（A 的边际）：$r_i = \sum_j n_{ij}$
- 列和（B 的边际）：$c_i = \sum_j n_{ji}$
- 差向量：$d_i = r_i - c_i$

**H0：边际同质性（marginal homogeneity）**

- 对所有 i，$r_i = c_i$
- 等价于 $d = 0$

解释：

- 它不要求每个样本预测一致  
- 只要求总体上每个类别的预测占比一致

---

### 2.2 为什么只用前 $K-1$ 维？

因为总数约束恒成立：

- $\sum_i r_i = \sum_i c_i = N$
- 所以 $\sum_i d_i = 0$

这表示 d 的 K 个分量中，最后一个由前 $K-1$ 个决定：

- $d_K = -\sum_{i=1}^{K-1} d_i$

因此自由度是 $K-1$，只取 $d_{vec} = (d_1, \dots, d_{K-1})$。

---

### 2.3 为什么要构造 V 矩阵？

多分类里，差向量 d 是“多维的”，且各维之间会相关（一个类别变多通常挤占另一个类别）。
因此不能用简单的 $\sum d_i^2$ 来衡量异常程度，需要用一个“噪声规模与相关结构”的矩阵来标准化。

Stuart–Maxwell 构造 $(K-1) \times (K-1)$ 的矩阵 V：

- 对角（类别 i 自身波动规模）：
  - $V_{ii} = r_i + c_i - 2n_{ii}$
- 非对角（类别 i 与 j 的直接交换耦合）：
  - $V_{ij} = -(n_{ij} + n_{ji})$，当 $i \ne j$

直觉：

- $n_{ij} + n_{ji}$ 越大，说明 i 与 j 之间互相“换类”越多，导致 $d_i$ 与 $d_j$ 更相关

---

### 2.4 统计量 T 与 p-value

统计量定义为：

- $T = d_{vec}^T V^{-1} d_{vec}$

含义：

- 这是对边际差向量 d 做了“按其噪声结构标准化”后的异常程度（可理解为多维版 z-score 的平方）

在 H0 下，近似有：

- $T \sim \chi^2_{df=K-1}$

所以：

- $p = P(\chi^2_{K-1} \ge T)$（右尾概率）

---

### 2.5 完整手算案例（K=3，含非对角项）

#### 案例 counts（3×3）

我们选一个 3×3 的例子，并确保 $n_{01}+n_{10} > 0$ 让非对角项不为 0：

```text
counts =
[ [35,  8,  7],
  [ 2, 20,  3],
  [ 3,  6, 16] ]
```

总数：

- $N = 100$

---

#### Step 1：算行和 r 与列和 c

行和（A边际）：

- $r_0 = 35+8+7 = 50$
- $r_1 = 2+20+3 = 25$
- $r_2 = 3+6+16 = 25$

列和（B边际）：

- $c_0 = 35+2+3 = 40$
- $c_1 = 8+20+6 = 34$
- $c_2 = 7+3+16 = 26$

解释：

- A 预测为类0 的总次数是 50，但 B 预测为类0 的总次数是 40  
- 边际上已出现差异

---

#### Step 2：算差向量 d = r − c

$$
d = [50-40,\; 25-34,\; 25-26] = [10,\; -9,\; -1]
$$

验证约束：

- $10 + (-9) + (-1) = 0$ 成立

取前 $K-1=2$ 维：

- $d_{vec} = [10,\; -9]^T$

---

#### Step 3：构造 V（2×2）

对角项：

- $V_{00} = r_0 + c_0 - 2n_{00} = 50+40-2\cdot 35 = 20$
- $V_{11} = r_1 + c_1 - 2n_{11} = 25+34-2\cdot 20 = 19$

非对角项：

- $V_{01} = -(n_{01}+n_{10}) = -(8+2) = -10$
- $V_{10} = -10$

因此：

```text
V =
[ [ 20, -10],
  [-10,  19] ]
```

解释：

- 对角 20、19：类0与类1 自身边际波动规模  
- 非对角 -10：类0与类1 之间交换较多（8 与 2），导致两维差异量相关（耦合）

---

#### Step 4：求 $V^{-1}$

2×2 求逆：

- $\det(V) = 20\cdot 19 - (-10)\cdot(-10) = 380 - 100 = 280$

所以：

```text
V^{-1} = (1/280) * [ [19, 10],
                    [10, 20] ]
```

---

#### Step 5：计算 $T = d_{vec}^T V^{-1} d_{vec}$

先算 $V^{-1} d_{vec}$：

- 第一行：$(19\cdot 10 + 10\cdot(-9))/280 = (190 - 90)/280 = 100/280 = 0.3571429$
- 第二行：$(10\cdot 10 + 20\cdot(-9))/280 = (100 - 180)/280 = -80/280 = -0.2857143$

所以：

- $V^{-1} d_{vec} = [0.3571429,\; -0.2857143]^T$

再算 T（点积）：

- $T = [10,\; -9]\cdot[0.3571429,\; -0.2857143] = 10\cdot 0.3571429 + (-9)\cdot(-0.2857143)$
- $= 3.571429 + 2.571429 = 6.142858$

得到：

- $T \approx 6.1429$
- $df = K-1 = 2$

解释：

- T 是“标准化后的边际差异强度”。  
- 如果边际差异只是随机波动，T 一般不会太大；T 越大越反常。

---

#### Step 6：从 T 得到 p-value（df=2 的快速计算）

p-value 定义为：

- $p = P(\chi^2_{2} \ge T)$

当 $df=2$ 时，有闭式右尾概率：

- $p = e^{-T/2}$

代入 $T=6.142858$：

- $p = e^{-3.071429} \approx 0.0464$

结论：

- 若 $\alpha=0.05$，因为 $p \approx 0.0464 < 0.05$，拒绝 H0  
- 说明 A 与 B 的边际分布差异足够大，难以用随机波动解释（存在系统性差异）

---

## 3) 快速对照总结（写报告可直接引用）

### McNemar（2类）

- 检验对象：不一致方向是否对称（b=c）
- 核心数据：只用 b 与 c
- p-value：在 H0 下 $X\sim \text{Binomial}(b+c, 0.5)$，计算“这么偏/更偏”的双侧概率

### Stuart–Maxwell（多类）

- 检验对象：边际同质性（行和=列和，即 d=0）
- 核心数据：差向量 $d=r-c$，以及由 counts 构造的 V（噪声规模与相关结构）
- 统计量：$T=d_{vec}^T V^{-1} d_{vec}$
- p-value：在 H0 下 $T\sim \chi^2_{K-1}$，计算右尾概率

---

## 4) 显著性解释口径（推荐）

当 $p<0.05$ 时，可严谨表述为：

> 在“两个模型预测行为没有系统性差异”的原假设成立时，出现当前这么极端的（不一致方向偏差 / 边际分布差异）概率很低，因此我们有统计证据认为两者预测行为确实存在系统差异。

同时建议注明：

> 显著性表示“差异不太可能仅由随机波动导致”，不等价于“性能更好”。性能优劣需结合准确率/F1等指标与差异方向综合判断。
