# Statistical Analysis

This module summarizes significance results and performs benchmark capability drop tests.

Primary entrypoints:

```bash
python run_statistical_analysis.py --model-root <model-root>
python run_benchmark_drop_test.py --input benchmark_deltas_by_dataset.md
```

Generated tables and figures are written to runtime output directories and are not included in this package.
